# Elliots Cars

A small 3D car parking game built with Three.js and Vite. The Volvo model is included in `volvo_v70.glb`.

## Run locally

```sh
npm install
npm run dev
```

Open the local address printed by Vite. To create a production build, run `npm run build`.
